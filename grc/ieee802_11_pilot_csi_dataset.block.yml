# auto-generated by grc.converter

id: ieee802_11_pilot_csi_dataset
label: Pilot+CSI Dataset
category: '[IEEE802.11]'

parameters:
-   id: filename
    label: Filename
    dtype: string
    default: '"pilot_csi_dataset.csv"'
-   id: append
    label: Append
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: [Enable, Disable]

inputs:
-   domain: message
    id: pilots
    optional: false
-   domain: message
    id: csi
    optional: false

outputs: []

templates:
    imports: import ieee802_11
    make: ieee802_11.pilot_csi_dataset(${filename}, ${append})

file_format: 1
